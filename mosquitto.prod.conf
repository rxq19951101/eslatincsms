# Production Mosquitto baseline: fail closed until the deployment supplies
# authenticated TLS listeners and per-device ACLs.

per_listener_settings true

listener 1883 0.0.0.0
allow_anonymous false

listener 9001 0.0.0.0
protocol websockets
allow_anonymous false

log_dest stdout
log_type all
persistence true
persistence_location /mosquitto/data/
