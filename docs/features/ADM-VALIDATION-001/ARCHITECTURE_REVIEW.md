# 架构审查摘要

- 表单规则散落在页面，后端 Schema 多数只有基础类型。
- 远程控制端点缺少明确权限依赖，且 Admin 与 API 字段命名不一致。
- 站点、充电桩及多个账务实体混用字符串、时间戳和外部业务标识作为主键。
- 任意 `config_key/config_value` 可写入，存在配置污染风险。
- 租户开通由前端串联三个事务，可能留下部分数据。
- 当前方形 PNG 品牌图在 48px 侧栏中不可读，且白底破坏深色界面。
