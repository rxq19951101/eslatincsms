# Codex 多 Agent 工作规范

## 当前模式

- `ARCHITECTURE_FIRST_MODE` 已由项目负责人明确禁用。
- 普通功能、缺陷和设计任务可以按本规范执行。
- 不得因历史上下文自行重新启用架构优先模式；只有项目负责人可以重新启用。

## 角色与职责

- Codex 根主线程担任产品经理和工作流调度角色。
- `frontend_engineer` 负责 App、Admin、前端页面、状态管理和前端测试。
- `backend_engineer` 负责服务端 API、设备接入、数据库、计费、权限和后端测试。
- `qa_engineer` 负责接口测试、集成测试、E2E、回归和缺陷报告。
- 主线程原则上不直接修改前后端业务代码；专业 Agent 不可用时必须先向项目负责人报告，不得连续尝试多个降级 Agent。

## 需求门禁

- 每个功能必须有需求编号，例如 `CHG-001`。
- 功能文档位于 `docs/features/<需求编号>/`。
- 开发前至少存在 `PRD.md`、`ACCEPTANCE.md`、`TASKS.md`，以及 `API_CHANGES.md` 或已更新的正式 API 契约。
- 文档必须标记 `status: ready-for-dev` 后，开发 Agent 才能修改业务代码。
- 固定顺序：产品规格 -> API 契约 -> 实现 -> 集成 -> QA -> 人工审查。
- 前端不得自行发明 API 字段；后端不得擅自改变已冻结 API。
- QA 不得通过静默修改产品代码让测试通过。

## 任务收敛门禁

- 每次只处理一个明确任务；完成后先向项目负责人确认，再进入下一项。
- 不得以“顺便统一”“顺便重构”为由扩大范围。
- 大型任务必须拆成可独立验收的小步骤，但不设置强制时间上限或定时停止点。
- 任务拆分优先按 API 契约、后端查询、导出、前端页面、测试等可独立验收单元进行；不得把多个可分离的实现任务打包给同一个 Agent。
- 只有共享契约、同一批核心文件或原子迁移无法安全拆开时，才可标记为“统一大任务”。启动前必须说明不可拆分原因、文件范围和统一验收标准。
- 小任务默认只修改直接相关文件；若需要扩大到全局主题、公共组件、数据库或 API，必须先说明影响并获得确认。
- 不明确的产品规则、权限边界或交互选择必须先问项目负责人。

## Agent 调度门禁

### 单 Agent 原则

- 同一任务最多启动一个实现 Agent。
- 不恢复旧 Agent 处理新任务。
- 不把同一任务重复发送给第二个 Agent。
- Agent 提示只包含需求编号、文档路径、文件所有权、时间限制和必须保留的已有改动；详细要求以需求文档为唯一来源。

### 超时规则

- Agent 启动 60 秒后最多检查一次实际文件变化。
- 60 秒没有文件变化时，只允许发送一次简短提醒。
- 普通拆分任务在 120 秒仍无文件变化时必须终止 Agent，并向项目负责人报告。
- 仅对启动前已声明且确实不可拆分的“统一大任务”，无文件变化超时可以提高到 300 秒；不得因为 Agent 运行缓慢而在执行中临时提高超时。
- 统一大任务在 120 秒时检查一次实际活动；确认仍在读取、分析或形成原子补丁时可继续等待，300 秒仍无文件变化必须终止并报告。
- 专业 Agent 不可用时不得自动尝试旧 Agent、通用 Worker或多层降级；必须先取得项目负责人指示。
- 不得用连续短等待、反复轮询或重复进度消息消耗时间和 Token。

## 文件所有权和并发门禁

同一时间，同一批文件只能有一个执行者。

### Agent 所有权

- Agent 开始执行后，其任务范围内文件的所有权为 `OWNER=agent`。
- 主线程只能查看状态、审查只读结果和处理不重叠工作。
- 主线程不得同时准备、应用或试写相同文件的补丁。

### 主线程接管

只有完成以下步骤后，所有权才能变为 `OWNER=main`：

1. 终止 Agent。
2. 确认 Agent 状态为 `shutdown`、`completed` 或 `failed`。
3. 重新读取 `git status`、目标文件和相关 diff。
4. 基于最新内容重新制定最小补丁。

- 接管后不得复用 Agent 运行期间准备的旧补丁。
- 优先按文件或单一逻辑单元应用小补丁，禁止未经最新校验的一次性超大补丁。
- `apply_patch` 校验失败后必须先重新读取文件，不得立即生成另一份大补丁。

## Token 和工具输出控制

- 使用 `rg` 或 `rg --files` 时必须把搜索范围限定在当前工作区和直接相关目录，禁止搜索父目录或用户主目录。
- 先使用 `git diff --stat` 或限定文件的 diff，再决定是否读取具体补丁。
- 不得重复读取已经完整读取且未变化的文件。
- 不得把完整需求重复写入多个 Agent 提示；需求文档是唯一事实来源。
- 构建和测试输出应限制 Token，只保留错误上下文和最终摘要。
- 临时资产只生成一次，并在任务结束时清除重复和中间文件。

## 测试和构建门禁

- 单项任务默认只执行与改动直接相关的测试、类型检查和最小人工验证。
- 测试未完成或发现新问题时，先报告问题并由项目负责人决定下一步，不得自动扩展为全量回归。
- 只有项目负责人明确要求，或修改涉及公共基础设施时，才执行全量回归。
- 多个问题应先全部修复，再统一重建容器或镜像，禁止每修一个问题就重复构建。
- 不得在没有实际测试证据时声称“完全通过”或“没有回归”。

## 安全与领域约束

- 不部署生产环境，不操作生产数据库。
- 不提交密码、Token、证书或私钥。
- 所有业务数据必须考虑 `tenant_id` 和多租户隔离；后端不能依赖前端传入的 `tenant_id` 作为最终授权依据。
- App 默认可查看所有公开充电桩；后台交易、账务和结算按租户隔离。隐藏公开充电桩功能尚未开发。
- 金额不得使用二进制浮点数。
- 后端时间统一保存为 UTC。
- 设备事件、支付回调和远程指令必须考虑幂等性。
- 充电会话必须使用明确状态机并校验状态迁移。
- 关键运营操作必须有审计日志。
- 不得擅自假设 OCPP 或其他设备协议版本。

## 工作区保护

- 当前工作区可能包含项目负责人的未提交修改，必须保留。
- 不得回退、覆盖或清理不属于当前任务的改动。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性命令，除非项目负责人明确要求。
- 文件编辑优先使用 `apply_patch`。

## 统一交接格式

每个 Agent 完成后必须返回：

```text
STATUS: done | blocked | failed

CHANGED_FILES:
- 修改文件

COMMANDS_RUN:
- 执行命令

TEST_RESULTS:
- 测试结果

CONTRACT_CHANGES:
- API、公共类型、数据库是否变化

RISKS:
- 风险、未完成事项和需要人工确认的内容
```
